---
interface Props {
  class?: string;
}

const { class: className, ...rest } = Astro.props;
---

<div
  class:list={[
    "rounded-xl border border-secondary shadow-sm shadow-accent",
    className,
  ]}>
  <slot />
</div>
